export type User = {
  // personId: string;
  partyId?: string;
  username: string;
  name: string;
  givenName: string;
  surname: string;
};

export type ClientUser = {
  name: string;
  username: string;
};
